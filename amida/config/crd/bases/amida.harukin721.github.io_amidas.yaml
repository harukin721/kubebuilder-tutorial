---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: amidas.amida.harukin721.github.io
spec:
  group: amida.harukin721.github.io
  names:
    kind: Amida
    listKind: AmidaList
    plural: amidas
    singular: amida
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Amida is the Schema for the amida API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: 一般的にカスタムリソースのSpecはユーザーが記述するもので、システムのあるべき状態をユーザーからコントローラーに伝える用途として利用されます。
              AmidaSpec defines the desired state of Amida
            properties:
              results:
                description: RESULTS is the list to store the selected items
                items:
                  type: string
                type: array
              selectCount:
                description: SELECTCOUNT is the number of items to select
                format: int32
                type: integer
              selects:
                description: Amidaリソースの定義に結果を格納するためのフィールドを追加します。 SELECTS is the list
                  of items to select from
                items:
                  type: string
                type: array
            type: object
          status:
            description: カスタムリソースのStatusはコントローラーが記述するもので、実際のシステムの状態を表現するために利用されます。
              AmidaStatus defines the observed state of Amida
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
